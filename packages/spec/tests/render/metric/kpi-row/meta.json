{
  "level": 1,
  "tags": [
    "metric",
    "kpi"
  ],
  "note": "SPEC 8.13: one hero, deltas signed and naming their period, goodDirection deciding the colour rather than the sign. It also pins the silence around a tile that states its number outright: `format:` on a metric is a number format (SPEC 8.13), not the data-section format enum (SPEC 6.2), and a block that never needed a data section gets an implicit empty one rather than MDV2100. Three blocks, no diagnostics.",
  "pin": [
    "ast",
    "diagnostics",
    "svg"
  ]
}
